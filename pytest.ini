[pytest]
testpaths = tests
markers =
    browser: Visu-/Konfigurator-Test in Chromium (braucht Playwright, siehe requirements-dev.txt)
filterwarnings =
    # webvisu legt das App-Objekt wie aiohttp-Beispiele unter app["app"] ab
    ignore:It is recommended to use web.AppKey
